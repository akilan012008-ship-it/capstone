version: '3.8'

services:
  akhilanmart-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: akhilanmart-container
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xms256m -Xmx512m
    restart: always
